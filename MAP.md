# MAP.md (Archived)

This document is archived and is not a startup or execution source of truth.

Use these instead:
- `ROLES.md` for role behavior and startup baseline.
- Evidence Ledger + Team Memory for session truth and history.
- `.hivemind/build/*.md` and `.hivemind/context-snapshots/*.md` for active coordination state (legacy fallback: `workspace/`).
